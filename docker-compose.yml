version: '3.8'

services:
  stillool:
    build: .
    container_name: stillool-server
    ports:
      - "3000:3000"
    volumes:
      - ./uploads:/app/uploads
    environment:
      - PORT=3000
    restart: unless-stopped
